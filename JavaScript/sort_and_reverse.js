let numbers = [5,10,4,40];

numbers.sort();     // sort numbers according to strings as it converts numbers into strings

console.log(numbers);

// reverse the above numbers
numbers.reverse();
console.log(numbers);